# AI Music Player 配置示例
# 复制此文件为 .env 并修改对应值

# 本地音乐文件目录 (可选)
# 默认为项目根目录下的 music 文件夹
# MUSIC_DIR=/path/to/your/music

# SQLite 数据库文件路径 (可选)
# 默认为项目根目录下的 music.db
# 支持绝对路径或相对于项目根目录的路径
# DATABASE_PATH=music.db

# 默认音量 0.0~1.0 (可选)
# 0.0 表示静音，1.0 表示最大音量
# DEFAULT_VOLUME=0.7